{
    "scripts": {
        "heroku-prebuild": "cd app && npm install && npm run build",
        "start": "cd app && npm start"
    },
    "dependencies": {
        "@project-serum/anchor": "^0.22.1",
        "react-router-dom": "^6.2.2"
    },
    "devDependencies": {
        "chai": "^4.3.4",
        "mocha": "^9.0.3"
    }
}
